/** Shared non-secret env key names — values live in .env (see apps/web/.env.example). */
export const ENV_PUBLIC_SUPABASE_URL = 'NEXT_PUBLIC_SUPABASE_URL' as const;
export const ENV_PUBLIC_SUPABASE_ANON_KEY = 'NEXT_PUBLIC_SUPABASE_ANON_KEY' as const;
export const ENV_PUBLIC_APP_URL = 'NEXT_PUBLIC_APP_URL' as const;
