# scripts

Repo automation: sync edge functions, codegen, local helpers.

- **`init-web-env.sh`** — run via `npm run env:web` from repo root: creates `apps/web/.env.local` from `apps/web/.env.example` when missing (never overwrites).
