# Supabase Edge Functions

Deploy from the monorepo root with `supabase functions deploy <name>`.

Planned function names (spec + bootstrap): `ownerrez-oauth`, `ownerrez-webhooks`, `stripe-webhooks`, `analytics-events`. Source stubs are described under `apps/api/functions/`.

See [`docs/decisions/adr-0001-repo-structure.md`](../docs/decisions/adr-0001-repo-structure.md).
