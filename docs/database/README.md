# Database

- Migrations: `supabase/migrations/` (mirrored at `infra/supabase/migrations` via symlink — ADR-0001).
- Section 4 tables: `resorts`, `maps`, `sites`, `ownerrez_tokens`, `team_members`. All use RLS.
