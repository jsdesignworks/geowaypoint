# Tests

- `e2e/` — Playwright (config at repo root).
- `integration/` — API / DB integration tests.
- `widget/` — Embed bundle tests.
- `fixtures/` — Shared data.

Vitest is configured at repo root with `passWithNoTests: true` until specs are added.
