# Release v0.1.0 — Monorepo bootstrap

## Scope

- Monorepo layout (`apps/web`, `apps/api`, `apps/embed`, `packages/*`, `docs`, `infra`, `environments`, `tests`, `releases`, `.github/workflows` placeholders).
- Next.js 14 foundation: Sora + Fraunces, spec §3 CSS tokens, UI stubs, Sentry init, baseline `logger`.
- Supabase CLI project init; Section 4 migration with RLS (`resorts`, `maps`, `sites`, `ownerrez_tokens`, `team_members`).
- Embed `embed.min.js` build (esbuild, framework-free).
- ADR-0001 (repo layout, migrations path, Edge Function canonical location).

## Database

- `supabase/migrations/20260409120000_section4_core.sql` — Section 4 only.

## Environment

- `apps/web/.env.example`, `apps/api/.env.example`, `environments/*/README.md`.

## QA verification

- [ ] `cd geowaypoint && npm install`
- [ ] `npm run build --workspace=@geowaypoint/web`
- [ ] `npm run embed:build`
- [ ] `npm run typecheck --workspaces --if-present`
- [ ] `supabase db push` (or equivalent) against a dev project

## Rollback

- Remove migration file before first production apply; if already applied, add forward-only migration (ADR required for destructive rollback).
