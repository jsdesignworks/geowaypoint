export default function OverviewPage() {
  return (
    <div>
      <p style={{ color: 'var(--ink3)', maxWidth: 560 }}>
        Overview — resort summary and quick links will match spec §6–§7 as data wiring lands. Use the
        sidebar to open Maps, Editor, Analytics, and Settings.
      </p>
    </div>
  );
}
